package com.cst8277.ums.ojo.johnson.data.models;

//In Java, enum is used as a constant. Implicitly, the properties are final and static.
// The main purpose of enums is to indicate all the constants that won't be changed.
public enum Role {
    ADMIN,
    SUBSCRIBER,
    PRODUCER
}
